Bootstrap: docker
From: python:3.12-slim

%post
    # Set noninteractive frontend
    export DEBIAN_FRONTEND=noninteractive

    # Install only Python packages we need
    pip install numpy

%environment
    export PYTHONPATH=/opt:$PYTHONPATH

%runscript
    python3 "$@"

